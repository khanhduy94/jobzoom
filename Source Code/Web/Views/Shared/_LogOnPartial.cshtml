@if(Request.IsAuthenticated) {
    <text>Welcome <b>@Context.User.Identity.Name</b>!
    [ @Html.ActionLink("Log Out", "Logout", "Users") ]</text>
}
else {
    @:[ @Html.ActionLink("Log In", "Login", "Users") ]
}
